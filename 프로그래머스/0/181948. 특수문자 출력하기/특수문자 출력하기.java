// Solution 클래스 정의
public class Solution { 

    // main 메서드는 프로그램 실행의 시작점
    public static void main(String[] args) { 
        // 특수문자를 포함한 문자열을 출력함
        // 백슬래시(\), 작은따옴표('), 큰따옴표(")는 이스케이프 문자로 처리해야 함
        // 문자열 내에서 백슬래시는 \\, 작은따옴표는 \', 큰따옴표는 \" 로 작성해야 함
        System.out.print("!@#$%^&*(\\'\"<>?:;");
    }
}
